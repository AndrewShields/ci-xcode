name: Check MacOS version

on:
  workflow_dispatch:

jobs:
  check-macos-version:
    runs-on: macos-26

    steps:
      - name: Log macOS version
        run: |
          sw_vers
          uname -a
